package com.example.daily.theme

import androidx.compose.ui.graphics.Color

// Primary palette — deep electric purple
val DailyPurple = Color(0xFF8B5CF6)
val DailyPurpleLight = Color(0xFFA78BFA)
val DailyPurpleDark = Color(0xFF7C3AED)

// Secondary — warm gold accent
val DailyGold = Color(0xFFF59E0B)
val DailyGoldLight = Color(0xFFFBBF24)
val DailyGoldDark = Color(0xFFD97706)

// Tertiary — electric cyan
val DailyCyan = Color(0xFF06B6D4)
val DailyCyanLight = Color(0xFF22D3EE)

// Surface/Background — deep space
val DailySurface = Color(0xFF0F0E17)
val DailySurfaceVariant = Color(0xFF1A1929)
val DailySurfaceElevated = Color(0xFF232136)
val DailySurfaceCard = Color(0xFF2A2940)

// On colors
val DailyOnSurface = Color(0xFFE8E6F0)
val DailyOnSurfaceVariant = Color(0xFF9896A8)
val DailyOnPrimary = Color(0xFFFFFFFF)

// Game feedback colors — Word Guess
val LetterCorrect = Color(0xFF22C55E)       // Green — right letter, right position
val LetterPresent = Color(0xFFEAB308)       // Yellow — right letter, wrong position
val LetterAbsent = Color(0xFF3F3D56)        // Muted — letter not in word
val LetterDefault = Color(0xFF4A4862)       // Untouched tile
val LetterInput = Color(0xFF5B5880)         // Current input row

// Status colors
val DailySuccess = Color(0xFF22C55E)
val DailyError = Color(0xFFEF4444)
val DailyWarning = Color(0xFFF59E0B)

// Streak/achievement
val StreakFlame = Color(0xFFFF6B35)
val StreakGlow = Color(0xFFFFD93D)
